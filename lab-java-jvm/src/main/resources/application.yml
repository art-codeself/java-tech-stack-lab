server:
  port: 8083

spring:
  application:
    name: lab-java-jvm
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

# Module: Java JVM
# Description: JVM Tuning, GC, Class Loading, OOM Scenarios
# Add middleware-specific configuration below

server:
  port: 8110

spring:
  application:
    name: lab-zookeeper
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

# Curator / Zookeeper configuration
zookeeper:
  connect-string: localhost:2181
  session-timeout: 5000
  connection-timeout: 3000

logging:
  level:
    com.codeself.lab.zookeeper: debug
server:
  port: 8110

spring:
  application:
    name: lab-zookeeper
  thymeleaf:
    cache: false
    prefix: classpath:/templates/
    suffix: .html

# Module: Zookeeper
# Description: Distributed Lock, Config Center with Curator
# Add middleware-specific configuration below
